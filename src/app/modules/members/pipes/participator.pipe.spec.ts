import { ParticipatorPipe } from './participator.pipe';

describe('ParticipatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ParticipatorPipe();
    expect(pipe).toBeTruthy();
  });
});
